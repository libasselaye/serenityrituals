# SerenityRituals

Landing page (FR) pour SerenityRituals — pratique d'harmonisation énergétique.

Site statique, sans build : React 18 + Babel Standalone via CDN.

## Lancer en local

```bash
python3 -m http.server 8080
```

Puis ouvrir `http://localhost:8080/SerenityRituals.html`.
